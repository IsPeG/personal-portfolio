---
import ThreeJS from "../../../public/images/threejs.svg";
import Symfony from "../../../public/images/symfony.svg";
import AstroIcon from "../../../public/images/astro.svg";
import Hydrogen from "../../../public/images/hydrogen.png";
import CodeIgniter4 from "../../../public/images/ci4.svg";
import N8N from "../../../public/images/n8n.svg";

interface Props {
  handle: string;
  name: string;
}

const { handle, name } = Astro.props;
---

<div class="tech-icon-container">
  {
    name == "Three.js" ? (
      <img src={ThreeJS.src} width="40" height="40" class="icon invert" />
    ) : name == "Symfony" ? (
      <img src={Symfony.src} width="40" height="40" class="icon invert" />
    ) : name == "Astro" ? (
      <img src={AstroIcon.src} width="40" height="40" class="icon invert" />
    ) : name == "Hydrogen" ? (
      <img src={Hydrogen.src} width="40" height="40" class="icon" />
    ) : name == "CodeIngiter 4" ? (
      <img src={CodeIgniter4.src} width="40" height="40" class="icon invert" />
    ) : name == "N8N" ? (
      <img src={N8N.src} width="40" height="40" class="icon invert" />
    ) : (
      <i class={`bx ${handle} icon`} />
    )
  }
  <span>{name}</span>
</div>

<style>
  .icon {
    font-size: 2.5rem;
    margin: 0 1.5rem;
  }

  .tech-icon-container {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  span {
    display: block;
    background-color: #00000020;
    padding: 2px 10px 0px 10px;
    text-align: center;
    min-width: 3rem;
    border-radius: 5px;
    margin-top: .5rem;
  }

  body.dark-mode span {
    background-color: var(--dark-mode-underline-color);
  }

  body.dark-mode .tech-icon-container .invert {
    filter: invert(1);
  }
</style>
